<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingTop="40dp"
    android:paddingBottom="40dp"
    android:paddingStart="30dp"
    android:paddingEnd="30dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <EditText
        android:id="@+id/editTextDetailVehicleName"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:hint="@string/vehicle_name"
        android:textSize="20sp"
        android:textColor="@color/black"
        android:background="@drawable/edit_text_background"
        android:autofillHints="false"
        android:inputType="text" />

    <EditText
        android:id="@+id/editTextDetailVehicleStartTime"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:hint="@string/vehicle_start_time"
        android:textSize="20sp"
        android:textColor="@color/black"
        android:background="@drawable/edit_text_background"
        android:autofillHints="false"
        android:inputType="number" />

    <Button
        android:id="@+id/buttonSaveChanges"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:background="@drawable/button_background"
        android:textAllCaps="true"
        android:text="@string/save_detail_changes"/>

    <Button
        android:id="@+id/buttonDeleteVehicle"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/button_background"
        android:textAllCaps="true"
        android:text="@string/delete_vehicle"/>

</LinearLayout>